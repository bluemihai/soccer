json.extract! player, :id, :first, :last, :email, :phone, :positions, :keeper, :attendance_estimate, :ideal_minutes, :why, :paid, :ebssl_card, :created_at, :updated_at
json.url player_url(player, format: :json)