%h4= "#{@formation.name} Formation"

/ = image_tag(@formation.image_url)

#field{ class: "#{@formation.id}"}  

- if @formation.positions && @formation.positions.keys.size > 0
  %hr
  - @formation.positions.each do |k, v|
    = "##{k} — #{v}"
    %br
